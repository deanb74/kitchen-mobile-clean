# HH-0000 Check 5 Post-Terminal-Closure Governed Measurement Instrument Architecture and Implementation Authority Review

**Status:** OUTCOME 1 - MINIMUM GOVERNED MEASUREMENT INSTRUMENT FIXED - ONE BOUNDED CONSTRUCTION-AND-IMMEDIATE-VALIDATION AUTHORITY GRANTED - CHECK 5 EXECUTION AUTHORITY WITHHELD
**Review date:** 2026-08-15
**Review type:** Strictly documentation-only post-terminal-closure instrument architecture and implementation Authority review
**Historical architecture review:** `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_RESPONSE_EVIDENCE_CAPTURE_CHECK_5_GOVERNED_MEASUREMENT_INSTRUMENT_ARCHITECTURE_AND_IMPLEMENTATION_AUTHORITY_REVIEW.md`
**Controlling terminal closure:** `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_RESPONSE_EVIDENCE_CAPTURE_CHECK_5_TERMINAL_CANONICAL_POLICY_CLOSURE_REVIEW.md`
**Closed canonical policy:** `HH-CHECK-5-CLOSED-MEASUREMENT-POLICY-2`, 422,230 canonical UTF-8 bytes, SHA-256 `68a54af89e7433d2da4a1d5fd8833b2f849b0276b62c7b8eba529348f8680c94`
**Governed implementation-source access:** None
**Instrument implementation:** Not begun by this review
**Check 5:** `UNMEASURED`
**Check 6:** `NOT_RUN`
**Implementation:** `UNACCEPTED`
**Authority effect:** One bounded instrument construction-and-immediate-no-source-validation Authority is granted; no instrument execution against governed source and no Check 5 or Check 6 Authority is granted

# Repository Traceability

**Principle:** Truth before certainty; evidence before claims; human Authority; smallest justified change.
**Theory:** A measuring instrument is a ruler, not a participant. It may encode closed policy and produce Evidence, but it may not infer policy from governed implementation or create semantic consequence.
**Architecture:** One exact machine-local, one-shot CommonJS artefact embeds the exact closed canonical policy, observes exactly two fixed source identities only under later Authority, performs complete prohibited-first and exact-one terminal classification, verifies deterministic off-terminal captures, emits one bounded manifest, and stops.
**Engineering:** Exact-byte construction, repository-anchored TypeScript `5.9.3`, no-source direct falsifiers, external identity binding, one-use cardinality, fail-closed behavior, separate static conformance review, separate readiness decision, and separate execution Authority.
**Milestone:** Not Applicable.
**Evidence:** The closed canonical policy, its terminal closure review, the historical architecture review, and this documentation-only Authority decision. No instrument-conformance, readiness, Check 5, Check 6, implementation-conformance, freeze, or acceptance Evidence is produced.

## 1. Sole Question and Decision

> What is the minimum governed measurement instrument required to measure Check 5 faithfully against the closed canonical policy, and what bounded implementation Authority, if any, should now be granted to create it?

**Outcome 1 is selected.**

The sole blocker recorded by the historical architecture review is resolved. The exact closed canonical policy now supplies the complete machine-encodable member, edge, probe, normalization, classification, capture, manifest, one-use, failure, PASS, and mandatory-stop values that the instrument was previously forbidden to invent.

The minimum faithful instrument is:

> one new machine-local, one-shot CommonJS artefact outside the repository, containing the exact 422,230-byte closed canonical policy payload, one fixed two-source measurement path, one deterministic off-terminal capture-and-verification path, one bounded manifest path, internal cardinality guards, an externally enforceable exact-byte identity, and no semantic consequence.

One Authority is granted to construct that artefact and immediately validate its mechanics using no governed implementation source. This Authority does not permit Check 5 execution. It is exhausted by the first completed construction-and-validation action or by the first failure.

## 2. Strict Review Boundary

This review only:

1. reconsidered the historical architecture review after terminal canonical-policy closure;
2. fixed the minimum instrument architecture;
3. determined the bounded implementation Authority necessary to create and immediately validate one exact proposed instrument; and
4. created this one formation record.

This review did not:

1. inspect, open, read, hash, parse, or edit either governed implementation source;
2. inspect, modify, execute, promote, or inherit readiness from the historical 7,843-byte readiness-only artefact;
3. create, inspect, validate, readiness-test, or execute the new instrument;
4. run Check 5 or Check 6;
5. run implementation tests, typecheck, ESLint, Git validation, integration, freeze, or acceptance;
6. add a repository script, generator, package, dependency, configuration, source file, or generated index; or
7. grant Authority for any semantic, programme, contribution, delivery, Memory, Learning, Reflection, feedback, or Action consequence.

## 3. Preserved Historical Governance

The historical architecture review remains unchanged Outcome 3 Evidence. Its decision was correct for the evidence available on 2026-08-14: no closed machine-encodable policy then existed, so implementation Authority was withheld.

This fresh review does not overwrite, rename, amend, or supersede that record in place. It records a later decision after the historical review's sole stated dependency was separately resolved.

The historical readiness-only artefact remains:

```text
path=/tmp/hh-check5-proposed-instrument-20260814.cjs
byteLength=7843
sha256=b80041831183e5998bb4eb03064daadabce07dc0853175c85a0bfbd49a4aba3c
classification=READINESS_ONLY_NONCONFORMING_INSTRUMENT_EVIDENCE
inheritanceIntoNewInstrument=PROHIBITED
modificationOrPromotion=PROHIBITED
```

Its `INSTRUMENT_READY` result applies only to those historical bytes and does not establish readiness or conformance of the new instrument.

## 4. Resolved Dependency

The historical review required one closed machine-encodable policy before implementation. Terminal closure now fixes exactly:

```text
schema=HH-CHECK-5-CLOSED-MEASUREMENT-POLICY-2
canonicalByteLength=422230
canonicalSha256=68a54af89e7433d2da4a1d5fd8833b2f849b0276b62c7b8eba529348f8680c94
D5=37/37
D4=95/95
D6=22/22
terminalTotal=117/117
terminalUniqueIds=117/117
```

That identity closes the missing policy values without governed implementation observation. The new instrument may embed and enforce those exact canonical bytes. It may not rewrite, normalize into a different normative payload, complete, correct, relax, extend, or reinterpret them.

Any implementation need for new policy meaning, an omitted classification, an unresolved ambiguity, or a payload with a different canonical identity is not an implementation detail. It immediately stops this Authority and requires a separate policy-governance decision.

## 5. Minimum Faithful Instrument

### 5.1 Form and location

The instrument must be exactly one new machine-local `.cjs` artefact outside the repository. It must not be a repository support script, package command, source module, generator, generated repository artefact, extension of the readiness-only artefact, or multi-file executable mechanism.

CommonJS is required so the artefact can use `module.createRequire(<repository package.json>)` and bind TypeScript loading to the repository-installed compiler without a bare temporary-directory ESM import or global fallback.

### 5.2 Fixed policy and source inputs

The artefact must contain the exact closed canonical policy payload identified in Section 4 and reject any policy identity difference.

It must define exactly these two governed source identities and no third source:

```text
path=scripts/academy/support/responseEvidenceCapture.ts
bytes=16125
sha256=f986633d8dd0984b385320f61a2d4a38f593284690db3d03ff4d3f10614aacfe

path=scripts/academy/support/__tests__/responseEvidenceCapture.test.ts
bytes=25324
sha256=6b56cae73f1dcf8db64bb9a41137b16d46897bc656802a6530a3fe45843eb53f
```

During a later separately authorised Check 5 execution, each source may be read completely exactly once. Raw byte length and SHA-256 must match before UTF-8 decode, parser creation, or AST use. Mismatch must stop without baseline adoption, retry, fallback, or alternate source selection.

The present construction-and-validation Authority does not permit either source read.

### 5.3 Measurement path

The artefact must implement the closed policy as one deterministic measurement path:

1. acquire and consume an exclusive one-use machine-local Authority token immediately before the first complete governed source read;
2. read and identity-gate each fixed source exactly once;
3. decode and parse each complete identity-matched source exactly once using repository-anchored TypeScript `5.9.3`;
4. perform exactly one governed traversal of each AST;
5. completely enumerate every governed node and record required D3-V2 facts and provenance;
6. evaluate all 37 D5 prohibited predicates first;
7. apply exactly one of the 95 D4 or 22 D6 terminal predicates to each eligible record;
8. fail closed on prohibited findings, unknowns, overlaps, ambiguity, diagnostics, incomplete enumeration, duplicate identity, or cardinality difference;
9. derive deterministic canonical captures directly from the single in-memory enumeration;
10. independently close, reopen, reread, parse, count, hash, and structurally verify every capture;
11. recompute one bounded manifest only from verified captures; and
12. emit one bounded `PASS` or `FAIL` result and stop.

There may be no second read, parse, traversal, capture, manifest, invocation, retry, repair, alternate mode, fallback, or terminal reconstruction path.

### 5.4 Evidence transport

Complete enumerations must never be emitted to stdout or stderr. They must be written to an exclusive machine-local temporary root outside the repository using deterministic canonical serialization and complete-write handling.

Each capture must be closed and independently reopened before it can support the result. Verification must include schema, byte length, SHA-256, record count, record identities, canonical equality, and equality with the single in-memory measurement. A short write, zero-progress write, close failure, reread failure, parse failure, corruption, truncation, count difference, identity difference, or equality difference must stop without retry.

Only a bounded manifest may reach terminal output. The manifest must be independently recomputable from verified captures and contain the fixed source and policy identities, capture identities, complete counts, category totals, public-allowlist findings, diagnostic count, ambiguity count, stderr count, and one combined decision.

### 5.5 Closed consequence boundary

The instrument may observe, classify, transport Evidence, and report only. It may not:

1. edit governed implementation or any repository file;
2. append or modify governance records;
3. run Check 6;
4. perform acceptance, integration, harness, contribution, or delivery work;
5. invoke Andy, a provider, network, UI, clipboard, logging, response-capture, or action capability; or
6. create Memory, Learning, Reflection, feedback, retry, second-turn, or other semantic consequence.

Every precondition failure, instrument failure, `PASS`, and `FAIL` is terminal.

## 6. Why This Is the Minimum

Removing any of the following would make Check 5 unfaithful or its Evidence untrustworthy:

1. exact policy identity, because the instrument must not create its own baseline;
2. exact two-source identity gates, because the measured unit is fixed;
3. complete one-pass AST enumeration, because sampling cannot establish absence;
4. prohibited-first and exact-one terminal classification, because the closed policy requires total deterministic treatment;
5. deterministic off-terminal captures and independent reread verification, because bounded terminal output cannot carry the complete Evidence unit;
6. bounded manifest recomputation, because the reported decision must derive from verified captures;
7. one-use cardinality and mandatory stop, because retry or continuation would widen Authority; and
8. external exact-byte binding, because mutable program bytes cannot authorise themselves.

Adding a repository script, generator, second executable artefact, persistent service, generalized framework, or broader runtime would add governance and substitution surfaces without improving faithfulness. They are therefore outside the minimum.

## 7. Direct Falsification Required During Immediate Validation

The construction action must immediately validate the exact newly constructed bytes without reading either governed source. At minimum, synthetic or fixture-driven falsifiers must directly establish:

1. exact artefact byte length and SHA-256 can be independently measured and fixed;
2. exact closed-policy canonical byte length, SHA-256, schema, inventory, and terminal counts are embedded unchanged;
3. zero, one, duplicate, reordered, substituted, and third source configurations fail before source access;
4. source identity differences fail before decode, parser call, or AST traversal;
5. second read, parse, traversal, capture, manifest, invocation, and token use are rejected;
6. every closed enumeration kind, D3-V2 normalization path, D5 family, D4 terminal predicate, and D6 terminal predicate is exercised by governed synthetic fixtures;
7. unknown, overlap, ambiguity, diagnostic, omission, duplicate, and incomplete-enumeration mutants fail;
8. exact public/member/status allowlists pass and missing, extra, renamed, mutable, optional, or type-changed members fail;
9. every permitted, prohibited, ordinary-literal, denied-token-data, and authorised-probe structural class receives the closed expected result;
10. repeated canonical serialization is byte-equal;
11. short-write, zero-progress, close, reread, parse, count, identity, corruption, truncation, and structural-equality faults fail without retry;
12. manifest mutation, unverified input, direct in-memory substitution, oversized output, and recomputation differences fail;
13. stdout and stderr spies prove complete enumeration sentinels never reach terminal output;
14. wrong repository anchor, compiler path, TypeScript version, CommonJS loading mechanism, global compiler, `NODE_PATH`, and fallback compiler fail before source access;
15. one-byte instrument substitution fails an external exact-identity gate; and
16. denied import, API, path, and control-flow scans establish no broader or semantic consequence.

Validation must record operation counters proving:

```text
governedSourceOpenCount=0
check5MeasurementStarted=false
check5AuthorityConsumed=false
```

All falsifiers must target the same exact artefact bytes. No passing result may be inherited across a byte change.

## 8. Bounded Implementation Authority Granted

Exactly one Authority is granted for one indivisible action:

> construct one new machine-local one-shot CommonJS measurement artefact implementing the exact architecture in this review and the exact closed canonical policy, then immediately perform its complete no-governed-source mechanical and synthetic falsifier validation and record the exact artefact byte length, SHA-256, invocation contract, validation results, and zero-consumption counters.

This Authority permits only:

1. creating one new machine-local `.cjs` artefact outside the repository;
2. embedding the exact closed canonical policy bytes and fixed source identity constants;
3. implementing the minimum measurement, capture, manifest, cardinality, loading, failure, and stop paths;
4. creating ephemeral machine-local synthetic fixtures and captures required by immediate validation;
5. loading repository TypeScript `5.9.3` through repository-anchored `module.createRequire` for synthetic-only validation;
6. independently measuring the complete final artefact byte length and SHA-256;
7. running the no-source falsifiers in Section 7 against those exact final bytes; and
8. creating one separately governed result record for that completed construction-and-immediate-validation action if the action's governing instruction expressly requires it.

The implementation Authority is consumed at the first creation of the new instrument artefact. After consumption, any failure exhausts Authority. Correction, rewrite, byte change, alternate artefact, second validation invocation, retry, or fallback requires a fresh Authority decision.

Construction must stop before implementation-source access. No validation fixture may be derived from either governed source during this action.

## 9. Authority Withheld

This review grants no Authority for:

1. opening, reading, hashing, parsing, traversing, inspecting, testing, or editing either governed implementation source;
2. executing the new instrument against governed source or running Check 5;
3. adopting a changed policy, source, compiler, repository, or instrument identity;
4. modifying, promoting, or reusing the historical readiness-only artefact;
5. adding or modifying any repository code, package, task, configuration, generated index, or dependency;
6. instrument repair, rerun, alternate launch, alternate mode, replacement, or second candidate;
7. granting the instrument its own execution Authority or treating self-reported identity as sufficient;
8. Check 6 review or execution;
9. implementation conformance, integration, freeze, acceptance, or correction; or
10. harness work, Andy/provider invocation, response capture, contribution, delivery, Memory, Learning, Reflection, feedback, Action, or experiment execution.

## 10. Mandatory Failure and Stop Rules

The construction-and-validation action must stop immediately on the first:

1. need to invent, infer, relax, extend, or reinterpret policy;
2. difference from the exact canonical policy identity;
3. need to inspect governed implementation;
4. need for a repository edit, second executable artefact, generator, dependency, or broader footprint;
5. artefact byte change after identity fixing;
6. compiler-loading or environment difference;
7. failed or ambiguous falsifier;
8. inability to prove zero governed-source access and zero Check 5 Authority consumption; or
9. request for correction, retry, alternate launch, or substitution.

Failure does not permit repair under this Authority. It must be recorded honestly as the terminal result of the one action.

Successful construction and immediate validation also stop. Success establishes only one exact proposed instrument identity and its no-source validation Evidence. It does not establish static measurement conformance, execution readiness against governed source, Check 5 Authority, Check 5 PASS, implementation conformance, or acceptance.

## 11. Required Post-Construction Gates

If and only if construction and immediate validation complete successfully, the next separately governed gate is:

```text
HH-0000 CHECK 5 EXACT INSTRUMENT MEASUREMENT-CONFORMANCE REVIEW
```

That review must bind to the complete final artefact byte length and SHA-256 and inspect those exact bytes against every requirement in this record and the exact closed canonical policy. It may not execute Check 5.

Static conformance success does not itself grant execution Authority. A separate no-source readiness decision over the same exact bytes and a later separate exact-instrument execution Authority review remain required before any governed source access:

```text
exact construction and immediate no-source validation
  -> exact-byte static measurement-conformance review
  -> exact-byte no-source readiness decision
  -> separate exact-instrument Check 5 execution Authority review
  -> external exact-byte gate
  -> one Check 5 measurement
  -> mandatory stop
```

Any byte difference between gates breaks inheritance and returns to a fresh Authority decision.

## 12. Outcome Decision

### Outcome 1 - Minimum architecture fixed and bounded implementation Authority granted

**Selected.** Terminal policy closure resolves the only deeper dependency identified by the historical architecture review. One exact machine-local artefact can now encode rather than invent the complete policy, and its construction can be directly falsified without governed source access.

### Outcome 2 - An existing mechanism already satisfies the complete contract

Not selected. The historical 7,843-byte artefact is permanently readiness-only and statically nonconforming. No existing governed measurement instrument identity is established.

### Outcome 3 - A deeper dependency still prevents implementation

Not selected. No unresolved policy value required by the minimum architecture is identified after terminal closure. Any new policy need encountered during implementation is a future stop condition, not Authority to improvise.

## 13. Final Governance State

```text
historicalArchitectureReview=OUTCOME_3_PRESERVED_UNCHANGED
terminalCanonicalPolicy=CLOSED
canonicalPolicySchema=HH-CHECK-5-CLOSED-MEASUREMENT-POLICY-2
canonicalPolicyBytes=422230
canonicalPolicySha256=68a54af89e7433d2da4a1d5fd8833b2f849b0276b62c7b8eba529348f8680c94
minimumInstrumentForm=ONE_NEW_MACHINE_LOCAL_ONE_SHOT_COMMONJS_ARTEFACT
instrumentConstructionAndImmediateNoSourceValidationAuthority=GRANTED_ONCE
instrumentImplementation=NOT_BEGUN_BY_THIS_REVIEW
instrumentStaticConformance=NOT_REVIEWED
instrumentReadiness=NOT_ESTABLISHED_FOR_NEW_IDENTITY
instrumentExecutionAuthority=NONE
governedImplementationSourceAccess=NONE
check5=UNMEASURED
check6=NOT_RUN
freeze=NOT_PERFORMED
acceptance=NOT_PERFORMED
implementation=UNACCEPTED
```

```text
OUTCOME 1 - TERMINAL CANONICAL POLICY CLOSURE RESOLVES THE HISTORICAL INSTRUMENT-IMPLEMENTATION BLOCKER - MINIMUM FAITHFUL INSTRUMENT IS ONE NEW MACHINE-LOCAL ONE-SHOT COMMONJS ARTEFACT OUTSIDE THE REPOSITORY EMBEDDING EXACT CLOSED POLICY HH-CHECK-5-CLOSED-MEASUREMENT-POLICY-2 AT 422230 CANONICAL UTF-8 BYTES SHA256 68A54AF89E7433D2DA4A1D5FD8833B2F849B0276B62C7B8EBA529348F8680C94 - EXACTLY TWO FIXED SOURCE IDENTITIES ONE READ ONE PARSE ONE TRAVERSAL COMPLETE D3-V2 ENUMERATION PROHIBITED-FIRST THIRTY-SEVEN D5 EVALUATION EXACT-ONE NINETY-FIVE D4 OR TWENTY-TWO D6 TERMINAL CLASSIFICATION DETERMINISTIC OFF-TERMINAL CAPTURE INDEPENDENT REREAD VERIFICATION ONE BOUNDED MANIFEST ONE-USE CARDINALITY EXTERNAL EXACT-BYTE GATE AND MANDATORY STOP REQUIRED - ONE CONSTRUCTION-AND-IMMEDIATE-NO-SOURCE-VALIDATION AUTHORITY GRANTED AND CONSUMABLE ON FIRST ARTEFACT CREATION - ANY FAILURE OR BYTE CHANGE EXHAUSTS AUTHORITY WITHOUT REPAIR RETRY FALLBACK OR SUBSTITUTION - HISTORICAL 7843-BYTE READINESS-ONLY ARTEFACT AND OUTCOME 3 REVIEW PRESERVED UNCHANGED - NO GOVERNED SOURCE ACCESS - NO CHECK 5 EXECUTION AUTHORITY - CHECK 5 UNMEASURED - CHECK 6 NOT RUN - IMPLEMENTATION UNACCEPTED - SUCCESS STOPS BEFORE SEPARATE EXACT-BYTE STATIC CONFORMANCE READINESS AND EXECUTION-AUTHORITY GATES - STOP
```

Post-terminal-closure governed measurement instrument architecture and implementation Authority review stops here.