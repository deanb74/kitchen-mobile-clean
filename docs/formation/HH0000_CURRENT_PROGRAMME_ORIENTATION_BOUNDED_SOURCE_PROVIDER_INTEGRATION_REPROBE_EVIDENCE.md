# HH-0000 Current Programme Orientation Bounded Source-Provider Integration Re-Probe Evidence

**Status:** EVIDENCE ACQUIRED - FRESH INDEPENDENT ACCEPTANCE REQUIRED
**Evidence date:** 2026-08-12
**Classification:** A. INTEGRATION SOURCE CLOSURE DEMONSTRATED WITHIN THE AUTHORISED SYNTHETIC BOUNDARY
**Self-acceptance:** Prohibited and not performed
**Production effect:** None
**Contribution effect:** None

## 1. Controlling Authority

This Evidence was acquired under:

`docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_BOUNDED_SOURCE_PROVIDER_INTEGRATION_REPROBE_AUTHORITY_REVIEW.md`

The controlling status was:

`OUTCOME 1 - ONE EXACT SYNTHETIC INTEGRATION RE-PROBE AUTHORISED`

The prior Evidence remains unchanged at:

`docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_BOUNDED_SOURCE_PROVIDER_SYNTHETIC_TEST_EVIDENCE.md`

The retrieval activation clarification remains unchanged at:

`docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_ANDY_REPOSITORY_RETRIEVAL_ACTIVATION_BOUNDARY_REVIEW.md`

This record reports Evidence only and does not accept itself.

## 2. Pre-Edit Authority Gate

The corrected mechanical gate passed before any edit or Jest execution:

1. the controlling Authority existed with the exact Outcome 1 status;
2. the test file existed;
3. the old literal appeared exactly twice;
4. the new literal appeared zero times;
5. this fresh Evidence path did not exist;
6. the inline provider, instrumentation, fixtures, manifest, 36 assertions, single public invocation, and cleanup matched the Authority-reviewed shape;
7. covered production files had no working-tree change;
8. no real programme input, source, manifest, Case 001 content, or C18 content appeared in the test.

An initial auxiliary check incorrectly expected 40 `expect(` occurrences and reported an assertion/structure mismatch. A read-only count established the reviewed test contains 36. The gate was corrected to the observed reviewed count and passed before any edit or Jest execution. This auxiliary check was not an Authority precondition and caused no source change or invocation.

The pre-edit test hash was:

`e6744136a2f6d37d6026b0673792e806aae85570dcd5ce7c6b4cf896ed11bd90`

## 3. Exact Test Amendment

Only `lib/academy/__tests__/boundedSourceProvider.test.ts` was modified.

At exactly two existing occurrences, this literal:

`Explain the fictional lantern catalogue boundary.`

was replaced with:

`What does the fictional lantern catalogue boundary mean?`

The occurrences were:

1. the `runConstitutionalExamination` argument;
2. the matching provider query-history expectation.

No assertion, provider logic, fixture, manifest, instrumentation, cleanup, import, production source, or other test character was intentionally changed.

## 4. Focused Diff Proof

Post-edit mechanical checks established:

1. old literal count: `0`;
2. new literal count: `2`;
3. assertion count: `36`;
4. public Andy invocation count: `1`;
5. focused diff changes: exactly two old-literal removals and two new-literal additions;
6. replacing the new literal back with the old literal in-memory reproduced the exact pre-edit hash.

The post-edit test hash was:

`1c92aa7ce6b5d175c0d6a1c862c9d3e92daf131af7d95fe9b89afd5b98ef6218`

The normalized post-edit hash was the pre-edit hash:

`e6744136a2f6d37d6026b0673792e806aae85570dcd5ce7c6b4cf896ed11bd90`

This proves the test amendment was limited to the two authorised literal replacements.

## 5. Focused Jest Result

The authorised Jest command was executed exactly once:

```text
npm test -- --runInBand lib/academy/__tests__/boundedSourceProvider.test.ts
```

Exact result summary:

```text
PASS lib/academy/__tests__/boundedSourceProvider.test.ts
Test Suites: 1 passed, 1 total
Tests:       1 passed, 1 total
Snapshots:   0 total
Time:        0.241 s, estimated 1 s
```

Exit code: `0`.

Jest was not rerun. No alternative query or repair pass was attempted.

## 6. Integration Retrieval Observation

The following were directly observed by passing runtime assertions:

1. `retrievalActive` was `true`;
2. the injected provider invocation count was `1` immediately after Andy returned;
3. the provider recorded exactly the authorised definition question;
4. the provider attempted exactly A and B;
5. the provider returned exactly A and B;
6. Andy's result contained exactly A and B.

The test used one fresh Andy instance and one fresh one-use provider. The test contains exactly one `runConstitutionalExamination` call.

## 7. Source-Closure Observation

| Falsifier | Evidence status | Result |
|---|---|---|
| Only A and B returned | Directly observed | Provider and Andy result arrays contained exactly `authorised/atlas-a.md` and `authorised/atlas-b.md`. |
| C inaccessible | Directly observed | Direct provider variants did not attempt or return C; the integration attempted-path set contained only A and B. |
| D inaccessible | Directly observed | Direct provider variants did not attempt or return D; the integration attempted-path set contained only A and B. |
| No wider traversal | Directly observed within the instrumented boundary | The `readdirSync` spy recorded no integration call, and provider attempted paths were exactly A and B. |
| Andy added no source | Directly observed | Andy's returned source-path set equalled the provider's returned set. |

The focused execution also reran the unchanged direct-provider assertions. They passed but do not replace or rewrite the prior direct-provider Evidence.

## 8. Provenance Observation

| Provenance property | Evidence status | Result |
|---|---|---|
| `id` | Directly observed | Andy returned `synthetic-atlas-a` and `synthetic-atlas-b`. |
| `sourcePath` | Directly observed | Andy returned the exact manifest-relative A and B paths. |
| Section | Directly observed | Andy returned `Lantern Catalogue` and `Beacon Catalogue`. |
| Exact text | Directly observed through `snippet` | Andy's snippets exactly equalled the authored UTF-8 text for A and B. |
| Exact provider fragment | Directly observed before integration | The unchanged direct-provider assertions established `fragment === authored text`. |
| Integration result fragment field | Statically supported, not separately asserted | Current mapping carries provider `fragment` into the retrieved result, but the integration assertion checks exact `snippet`, not the retrieved `fragment` field separately. |

No broader provenance claim is made.

## 9. Fallback / Expansion Observation

1. **No fallback service invocation - directly observed:** the spy on `RepositoryKnowledgeService.prototype.search` recorded no call while the injected subclass handled the query.
2. **One injected-provider search - directly observed:** invocation count was exactly `1` after Andy returned.
3. **No source expansion - directly observed:** Andy's source-path result was exactly A and B, equal to the provider result.
4. **No production fallback mechanism introduced - statically observed:** normalized baseline equality proves the provider and test mechanism were unchanged, and covered production files remained unchanged.

## 10. Memory / Reflection / Learning Observation

1. **Public Memory - directly observed:** `andy.memory.all()` was empty.
2. **Last Reflection - directly observed:** `andy.getLastReflection()` was `null`.
3. **Learning promotion - statically supported and inferred, not exposed through a separate Learning assertion:** the source route promotes confirmed Learning through the recommendation-ready Reflection path; the directly observed empty Memory and null Reflection show that path did not produce its public effects.

The result does not demonstrate Memory or Learning capability.

## 11. Follow-Up / Second-Turn Observation

1. **Automatic additional repository call - directly observed:** the provider count remained one successful search after Andy returned.
2. **Second public invocation - statically observed absent:** the test contains one `runConstitutionalExamination` call.
3. **Automatic follow-up - statically supported and indirectly observed:** no source path issues a second public turn, and no second provider search occurred during Andy's call.
4. **Private conversation-history entry - expected but not directly inspected:** the private entry may have been created and was discarded with the fresh Andy instance.
5. **Feedback event - not directly observed through a dedicated event assertion:** no feedback mechanism is called on the traced route, and no test or production change introduced one.

No claim is made about private state beyond this single discarded instance.

## 12. One-Use Expiry Observation

The following were directly observed:

1. Andy caused the provider's first and only successful search;
2. a deliberate second direct `search` call on the same provider returned an empty array;
3. total method invocation count then became `2`, distinguishing one successful use from one refused use;
4. attempted paths remained exactly A and B after refusal;
5. returned paths remained unchanged;
6. the refused call caused no additional source access.

One-use behavior belongs to the test provider, not to `AndyDigitalColleague` production state.

## 13. Filesystem / Write Observation

During the integration invocation, passing spies directly observed no call to:

1. `writeFileSync`;
2. `appendFileSync`;
3. `mkdirSync`;
4. `renameSync`;
5. `copyFileSync`;
6. `unlinkSync`;
7. `rmSync`.

The directory-traversal spy also observed no `readdirSync` call. Static checks confirmed the unchanged test setup contains four fixture writes and one directory creation, all rooted beneath an operating-system temporary directory, followed by cleanup of that test-created directory only.

This Evidence is bounded to the synchronous filesystem operations instrumented by the existing test and the statically traced route.

## 14. Validation Results

### Typecheck

Command executed exactly once:

```text
npm run typecheck
```

Result: passed with exit code `0` and no reported TypeScript errors.

### ESLint

Command executed exactly once:

```text
npx eslint lib/academy/__tests__/boundedSourceProvider.test.ts
```

Result: passed with exit code `0` and no output.

### Static Checks

Focused static checks passed and established:

1. production files remained unchanged;
2. normalized test content matched the pre-edit baseline exactly;
3. provider, assertions, fixtures, manifest, instrumentation, and cleanup remained unchanged;
4. old literal count was zero and new literal count was two;
5. actual programme content and the real programme-orientation request were absent;
6. `super.search` was absent;
7. no production fallback mechanism was introduced;
8. test setup writes remained under the operating-system temporary directory;
9. no Memory or Learning mutation was added by the test;
10. one Andy public invocation remained.

### Editor Diagnostics and Whitespace

Editor diagnostics reported no errors in the test file or this Evidence record. Final static and whitespace checks passed with no trailing whitespace.

The full repository suite was not run.

## 15. Limitations and Unknowns

1. The result applies only to the authorised synthetic provider, fictional documents, exact definition input, and one fresh Andy instance.
2. The retrieved integration `fragment` field was mapped by current source but not separately asserted; exact `snippet` and direct-provider fragment were asserted.
3. Learning absence is supported by empty public Memory, null Reflection, and source tracing rather than a separate public Learning-state assertion.
4. Feedback absence and private conversation-history cardinality were not directly exposed by dedicated runtime assertions.
5. Filesystem Evidence covers the synchronous operations instrumented by the existing test; no general external-side-effect claim is made.
6. No real programme source, real manifest, default broad service, Context Door, contribution, or production deployment was exercised.
7. Independent acceptance remains outstanding.

## 16. Claims Withheld

This Evidence does not establish:

1. real programme source access or real-manifest correctness;
2. programme-orientation contribution readiness or Authority;
3. live-use or Context Door readiness;
4. formation completion or relational Understanding;
5. closure of the default repository service;
6. production repository capability;
7. Memory or Learning capability;
8. programme judgement or answer quality;
9. milestone completion, deployment, certification, production readiness, or acceptance.

## 17. Evidence Classification

**A. INTEGRATION SOURCE CLOSURE DEMONSTRATED WITHIN THE AUTHORISED SYNTHETIC BOUNDARY**

Through the existing neutral definition route, one fresh Andy instance used the injected closed synthetic provider, received only the authorised synthetic source set, preserved the directly asserted provenance, did not broaden access within the instrumented boundary, created no observed prohibited persistent effect, and respected the provider's one-use boundary.

This is claim-bearing Evidence, not acceptance.

## 18. Independent Acceptance Requirement

Fresh independent review remains required.

The smallest next question is:

> Does fresh independent review accept that the authorised synthetic re-probe demonstrates exact integration source closure, provenance retention, no wider access, no prohibited persistent effects, and one-use behavior through the existing AndyDigitalColleague injection boundary?

Do not create the real manifest, inspect real programme records, ask Andy the real programme-orientation question, begin contribution, modify production, reopen Context Door, or infer broader capability.

Integration re-probe Evidence acquisition stops here.