# HH-0000 Bounded Comparative Understanding Human-Decision Aggregation Correction Authority Review

**Status:** OUTCOME 1 - ONE LOCAL HUMAN-DECISION-AGGREGATION CORRECTION AUTHORISED
**Review date:** 2026-08-12
**Review type:** Fresh documentation-only MARC and Cyril correction-Authority review
**Implementation effect:** One local private human-question aggregation correction and minimum neutral focused-test work are authorised but unconsumed
**Acceptance effect:** None - the failed multi-disagreement correction remains failed and unaccepted
**Contribution effect:** None - no real programme manifest was inspected, Andy was not invoked, no contribution was executed, and contribution Authority was not reconsidered

## 1. Purpose

This review asks whether one fresh bounded correction Authority may permit the smallest delta from the current failed multi-disagreement attempt so that independently formed unresolved Evidence remains complete while human-decision questions aggregate by the settled exact decision boundary.

Yes. Current private formation already performs the previously authorised per-disagreement work. The remaining defect is confined to the identity used by the human-question gate:

```text
current failed key
    = exact unresolved Observation IDs + inference basis

governed key
    = sorted(unique(sourceId values belonging to the unresolved relationship's supporting Observations))
```

The correction does not roll back per-disagreement formation. It changes only human-decision-question aggregation.

This record grants correction Authority only. It does not implement, execute, validate, evidence, accept, or contribute the correction.

## 2. Settled Governance

The controlling human-decision-boundary review settled:

**OUTCOME 1 - EXISTING ONE-QUESTION EXPECTATIONS ARE GOVERNED AND A DETERMINISTIC AGGREGATION RULE EXISTS**

The invariant is:

```text
unresolved Evidence relationship
    != automatically
separate human decision
```

Every material disagreement requires independent Evidence and unresolved assessment. A human-decision question represents the absent organisational intent, current Context, Authority, or decision that only a human can supply. It does not count disagreements.

## 3. Exact Review Boundary

This review used only:

1. `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_MULTI_DISAGREEMENT_CORRECTION_AUTHORITY_REVIEW.md`;
2. `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_MULTI_DISAGREEMENT_CORRECTION_EVIDENCE.md`;
3. `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_HUMAN_DECISION_BOUNDARY_REVIEW.md`;
4. `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_RELATIONSHIP_RELEVANCE_CORRECTION_AUTHORITY_REVIEW.md`;
5. `lib/academy/AndyDigitalColleague.ts`;
6. `lib/academy/__tests__/boundedComparativeUnderstanding.test.ts`.

No production or test file was edited. No Jest, adjacent test, typecheck, lint, Andy invocation, synthetic contribution, real contribution, or acceptance review was run. No real programme manifest or real programme source was inspected or assembled.

## 4. Historical Continuity

The durable chain remains:

```text
implementation
-> first acceptance Outcome 3
-> relationship-relevance correction
-> second acceptance Outcome 3
-> multi-disagreement correction Authority
-> correction attempt
-> new scenarios PASS
-> three existing question-count regressions FAIL
-> mandatory stop
-> human-decision-boundary review Outcome 1
-> this human-decision-aggregation correction Authority review
```

The failed multi-disagreement Evidence remains a permanent failed execution record. It must not be edited, replaced, appended into a success claim, or rewritten as though its focused validation passed.

Any future correction Evidence is additive history under the fresh Evidence path authorised by this record.

## 5. Current Mechanical Boundary

Fresh read-only inspection directly observed:

1. production SHA-256 is `c570db8b28ce4cb2cb5964430f2a95c8e9c05f2c62854a7ff05b3d0a74c7a387`;
2. focused-test SHA-256 is `bb6aff51a2635eff76a8d5ce783804c0c10371ed0eacfdeb09e1c68cad6702cd`;
3. private formation contains the attempted complete `apparentDisagreements` iteration;
4. the accepted `isExplicitRelationshipRelevantToDisagreement` helper remains present and is called for each current disagreement;
5. participating source IDs are already derived from each disagreement's exact Observation IDs;
6. qualification, possible-supersession, unresolved relationship, uncertainty, and Inference formation occur independently before or outside human-question aggregation;
7. the current human-question boundary key uses the unresolved relationship's Observation IDs plus inference basis;
8. the focused suite contains 13 test declarations: the 12 pre-attempt tests plus the additive multi-disagreement scenarios test;
9. the three governed one-question expectations remain unchanged;
10. this Authority path and the future correction Evidence path were absent before this review.

The two hashes above define the future correction's required pre-edit baseline. Any mismatch before implementation stops execution and requires fresh review. It must not be silently adopted.

## 6. Treatment of the Current Failed Attempt

The current failed-attempt state must not be assumed to require reversal.

Its per-disagreement loop remains within the consumed multi-disagreement Authority because it:

1. evaluates every already formed `apparent-disagreement`;
2. reuses the accepted exact relationship-relevance helper;
3. limits qualification inputs to participating sources;
4. independently forms applicable qualification;
5. independently forms possible supersession only as Inference;
6. independently forms unresolved relationships and uncertainty;
7. preserves distinct unresolved boundaries for different participating record sets.

The new scenarios passed during the failed execution. That PASS does not accept the attempted implementation, but it is Evidence that rollback to singular first-disagreement processing would reintroduce the earlier completeness defect.

The current failed attempt is therefore the authorised baseline for one smaller delta. It remains failed and unaccepted until the fresh correction passes every stage required by this record and later receives a separate independent acceptance review.

## 7. Exact Remaining Defect

Current question aggregation forms this identity:

```text
sorted(unresolved relationship Observation IDs + unresolved inference basis)
```

Status, date, scope, and state disagreements between the same supplied records necessarily have different Observation IDs. The current key therefore treats distinct Evidence relationships as distinct human decisions even when they resolve to the same exact participating source set.

The three focused regressions demonstrate the effect:

1. one same-source-set fixture expected one question and received two;
2. one same-source-set fixture expected one question and received three;
3. one same-source-set relationship-relevance fixture expected one question and received two.

The defect is not independent relationship formation. It is not unresolved Evidence formation. It is not Inference, uncertainty, qualification, explicit relationship relevance, rendering, or persistence. It is only the key guarding insertion into `humanDecisionQuestions`.

**Defect classification:** `LOCAL PRIVATE HUMAN-QUESTION AGGREGATION IDENTITY`.

## 8. Exact Governed Aggregation Rule

For each independently formed unresolved relationship, the correction must:

1. resolve only that unresolved relationship's existing `observationIds` against the already formed `observations` array;
2. collect only the matched Observations' exact `sourceId` values;
3. remove duplicate source IDs;
4. require at least two distinct source IDs;
5. sort the distinct source IDs deterministically;
6. form the boundary identity only from that sorted source-ID set;
7. emit at most one human-decision question for each identical boundary identity;
8. emit separate questions for non-identical source-ID sets.

Conceptually:

```text
apparent disagreement A
    -> unresolved relationship A
    -> source set {X,Y}

apparent disagreement B
    -> unresolved relationship B
    -> source set {X,Y}

apparent disagreement C
    -> unresolved relationship C
    -> source set {P,Q}

Human questions:

{X,Y} -> one question
{P,Q} -> one question
```

The boundary key must not include or infer from wording, metadata category, title, filename, path, date, rank, score, order, topic similarity, semantic meaning, question text, inference wording, or relationship ordinal.

## 9. Fail-Closed Rule

Question aggregation is permitted only when the unresolved relationship's existing supporting Observation IDs resolve to at least two distinct exact source IDs.

If fewer than two distinct source IDs are obtained, if an Observation ID cannot be resolved, or if a stable exact source set cannot otherwise be obtained from current provenance, the implementation must fail closed rather than inventing a boundary.

For this bounded correction, fail closed means the candidate question must not be semantically merged with another candidate. The implementation may retain that candidate as its own exact unresolved boundary using existing behavior, or stop if doing so cannot preserve the governed question contract without a new field or wider responsibility. It must not use a guessed title, path, category, wording, or topic as a substitute key.

No current governed fixture is known to require this fallback because formed cross-source disagreements already require at least two participating sources. The fallback remains mandatory protection, not a new expected capability.

## 10. Evidence That Must Not Be Deduplicated

The correction must not deduplicate, suppress, merge, relabel, or remove:

1. `apparent-disagreement` relationships;
2. independently formed `unresolved-relationship` relationships;
3. `ComparativeInference` values;
4. uncertainty entries;
5. qualification relationships;
6. possible-supersession Inferences;
7. attributable explicit relationships;
8. supporting Observation IDs.

Only insertion into `humanDecisionQuestions` may aggregate by canonical participating source-ID set.

The first unresolved relationship encountered for a canonical source set may remain the representative `unresolvedRelationshipId` on the single existing private question object. Every unresolved Evidence relationship remains independently present in the comparative account. No type change or multi-link field is authorised.

## 11. Exact Production Correction Authorised

The only authorised production file is:

`lib/academy/AndyDigitalColleague.ts`

Within the current failed-attempt block in `formComparativeUnderstanding`, the correction may only:

1. preserve the complete `apparentDisagreements` iteration;
2. preserve all current per-disagreement relevance and consequence formation;
3. preserve the existing `humanDecisionBoundaries` set or an equivalent local set;
4. replace only the current Observation-ID-plus-inference-basis question key with the Section 8 canonical source-ID-set key;
5. use existing `StructuralObservation.id`, `StructuralObservation.sourceId`, `ComparativeRelationship.observationIds`, local `observations`, and local participating-source data;
6. add only narrowly local exact derivation needed for that key;
7. preserve the existing question type, text, reason, ID behavior, and first representative unresolved relationship link;
8. fail closed under Section 9.

The correction must leave `isExplicitRelationshipRelevantToDisagreement` unchanged. It must not move, generalise, duplicate, or reinterpret that helper.

No helper or abstraction is required. If an existing private helper is technically necessary, implementation must stop because this Authority permits only local question-key derivation inside the current formation block.

## 12. Neutral Focused Regression Required

The only authorised test file is:

`lib/academy/__tests__/boundedComparativeUnderstanding.test.ts`

All 12 pre-attempt tests, all original 53 grouped falsifiers, every existing one-question expectation, and the additive multi-disagreement scenarios must remain present and must not be weakened, removed, skipped, renamed to obscure scope, or replaced.

Minimum additive or corrective work within the current additive multi-disagreement test, or one additional neutral test if clearer, must prove all three cases below. Existing passing assertions must be retained unless an exact equivalent is required solely to add the governed source-set assertions without changing expected behavior.

### 12.1 Scenario A - Same Record Set, Multiple Unresolved Evidence Relationships

Use several governed metadata disagreements across one exact participating source-ID set.

Assertions must establish:

1. every `apparent-disagreement` remains separately present;
2. every independently required `unresolved-relationship` remains separately present;
3. applicable qualification, possible-supersession, Inferences, and uncertainty remain present independently;
4. all those unresolved relationships resolve to the same canonical source-ID set;
5. exactly one human-decision question is emitted for that source set.

### 12.2 Scenario B - Different Record Sets

Use unresolved disagreements involving at least two different exact participating source-ID sets.

Assertions must establish:

1. each disagreement and unresolved relationship remains separately present;
2. each canonical source-ID set is exact and different;
3. one human-decision question is emitted for each different source-ID set;
4. no question from one set substitutes for or suppresses the other.

### 12.3 Scenario C - Mixed Resolved and Unresolved Relationships

Use one source set with an exact relevant explicit authored relationship and another source set without one.

Assertions must establish:

1. every apparent disagreement remains visible;
2. the exact relationship remains attributable Evidence;
3. only its own source set omits unresolved consequences and a human question;
4. the other source set retains its unresolved relationships, uncertainty, applicable Inferences, and one human question;
5. nothing from the resolved source set suppresses, merges with, or relabels the unresolved source set.

All fixtures must remain synthetic and programme-neutral. They must not depend on document order, date priority, rank, score, filename identity, wording similarity, or semantic meaning.

## 13. Why the Existing Tests Can Be Restored Without Weakening

The three previously failing exact one-question assertions already express the settled same-source-set boundary. They must pass unchanged.

The additive different-source-set scenario already requires questions linked to both independently unresolved relationships. Source-set aggregation preserves that behavior because its two canonical source sets differ.

The additive mixed scenario already proves that exact resolution of one record set does not suppress another unresolved set. Source-set aggregation occurs only after independent relevance and unresolved formation, so it does not change that behavior.

The required Scenario A adds the missing discriminating proof: several unresolved Evidence relationships remain independently visible even though their human questions aggregate to one.

The intended correction therefore strengthens the combined test boundary. It does not trade old expectations for new ones.

## 14. Minimum File Surface

The complete future correction task is limited to exactly:

1. `lib/academy/AndyDigitalColleague.ts` - one local private human-question aggregation correction;
2. `lib/academy/__tests__/boundedComparativeUnderstanding.test.ts` - minimum neutral source-set regression work;
3. `docs/formation/HH0000_BOUNDED_COMPARATIVE_UNDERSTANDING_HUMAN_DECISION_AGGREGATION_CORRECTION_EVIDENCE.md` - one fresh correction Evidence record.

No other production, test, Evidence, Authority, formation, provider, repository, public contract, package, schema, service, component, Theory, or architecture file is authorised.

If another file is required, implementation must stop. Outcome 1 does not widen itself.

## 15. Authority Granted and Consumption

**Authority granted:**

1. one production correction within Section 11;
2. minimum focused-test work within Section 12;
3. creation of the one fresh Evidence record in Section 14;
4. one implementation attempt;
5. the one-shot validation sequence in Section 18;
6. post-pass static validation and Evidence in Section 19;
7. pre/post production and test hash capture and focused reversible-diff proof.

Correction Authority is unconsumed at creation of this record. It is consumed on the first production edit made under it.

The test work may be authored in the same patch as the production correction or immediately before it, but no pre-correction test execution is authorised. Authority consumption remains the first production edit.

No repair cycle is granted. The settled source-set identity, current local key, unchanged existing expectations, and three neutral scenarios make the correction sufficiently narrow and falsifiable for one attempt.

## 16. Authority Withheld

This Authority withholds:

1. rollback of the current per-disagreement iteration;
2. restoration of singular first-disagreement processing;
3. deletion or aggregation of any Evidence relationship, Inference, uncertainty, or qualification;
4. changes to the exact relationship-relevance helper;
5. changes to extraction, explicit relationship parsing, comparison grouping, relationship formation, qualification inputs, possible-supersession conditions, unresolved conditions, rendering, activation, retrieval, prioritisation, deliberation, Reflection, Memory, Learning, recommendation, feedback, Action, retry, or automatic follow-on;
6. changes to private or public types;
7. provider or repository changes;
8. semantic, fuzzy, aliased, title-based, path-based, filename-based, category-based, wording-based, or topic-based question deduplication;
9. modification, removal, weakening, skipping, or replacement of an existing falsifier or governed expectation;
10. packages, dependencies, schemas, services, components, shared helpers, engines, Theory, or architecture;
11. correction of lint warnings or unrelated defects;
12. edits to the failed multi-disagreement Evidence;
13. real manifest access, real source inspection, Andy invocation, contribution execution, acceptance review, or contribution Authority reconsideration.

The prohibited methods `prioritizeDocumentsForUnderstanding`, `retrieveFromRepository`, `buildDeliberationRecord`, and `buildReflectionRecord` must remain unchanged.

## 17. Failure and Stop Rule

Before editing, implementation must verify the Section 5 hashes, current per-disagreement markers, 13-test baseline, all 12 pre-attempt tests, original 53 falsifiers, additive scenarios, unchanged exact relevance helper, absent fresh Evidence path, and no unexpected changed-file requirement.

Stop immediately and create the fresh Evidence record with the exact failure, without repair or rerun, if:

1. a pre-edit gate differs;
2. the source-ID set cannot be derived only from the unresolved relationship's exact existing Observation IDs;
3. fewer than two source IDs cannot be handled under the Section 9 fail-closed rule without semantic guessing or a new type;
4. any per-disagreement relationship, qualification, Inference, or uncertainty must change;
5. the exact relevance helper must change;
6. an existing test or expectation must be weakened;
7. any focused, adjacent, typecheck, or lint command exits non-zero;
8. another production, test, or documentation file is required;
9. a public contract, provider/repository contract, parser, package, schema, service, component, Theory, or architecture change is required;
10. real programme content or contribution execution is required;
11. a prohibited method or changed-file boundary differs after executable PASS.

No repair, correction, second patch, rerun, or reinterpretation is authorised after failure. Fresh human Authority would be required.

## 18. Required One-Shot Validation

After the one local correction and focused regression work, run exactly once in this order.

First:

```text
npm test -- --runInBand lib/academy/__tests__/boundedComparativeUnderstanding.test.ts
```

Second, only after focused PASS:

```text
npm test -- --runInBand lib/academy/__tests__/boundedSourceProvider.test.ts lib/academy/__tests__/deliberation.test.ts lib/academy/__tests__/reflection.test.ts lib/academy/__tests__/repositoryKnowledgeService.test.ts
```

Third, only after adjacent PASS:

```text
npm run typecheck
```

Fourth, only after typecheck PASS:

```text
npx eslint lib/academy/AndyDigitalColleague.ts lib/academy/__tests__/boundedComparativeUnderstanding.test.ts
```

Warnings must be recorded and not fixed. Any non-zero exit stops the task without repair or rerun.

## 19. Required Static Validation and Evidence

Only after all executable stages pass, perform:

1. editor diagnostics on production, focused test, and fresh Evidence;
2. focused production/test diff review;
3. mechanical reversal against the Section 5 hashes;
4. whitespace and final-newline checks;
5. changed-file boundary verification;
6. prohibited-method verification;
7. exact relevance-helper preservation verification;
8. production and focused-test post-hash capture;
9. exact preservation checks for the 12 pre-attempt tests, original 53 falsifiers, three one-question expectations, and additive multi-disagreement scenarios;
10. explicit counts proving that same-set question aggregation did not reduce apparent disagreements, unresolved relationships, Inferences, uncertainty, or qualification;
11. programme-neutrality checks.

The fresh Evidence record must preserve:

1. the complete historical chain, including the prior failed attempt;
2. the new Authority and exact consumption point;
3. all pre-edit gates and hashes;
4. the exact production and test delta from the current failed-attempt baseline;
5. scenarios A, B, and C and their exact results;
6. every command and result;
7. all post-pass static checks and hashes;
8. direct observations, Inferences, limitations, and explicit non-consequences;
9. the fact that fresh independent acceptance remains required.

The new Evidence must not modify the old failed Evidence or accept itself.

## 20. MARC Independent Finding

MARC asks:

> Would this result give the human every material uncertainty without repeatedly asking them the same underlying decision?

Yes. Independent apparent disagreements, unresolved relationships, uncertainty, and Inferences remain visible. The human receives one question for each exact participating record set rather than one repeated question for each metadata manifestation of the same absent decision.

Different record sets still produce separate questions. Exact relationship Evidence can resolve only its own participating set. The correction therefore preserves every material uncertainty while removing only repeated demands on the human.

**MARC finding:** `KEEP EVERY MATERIAL UNCERTAINTY VISIBLE; ASK ONCE PER EXACT PARTICIPATING SOURCE SET`.

## 21. Cyril Independent Finding

Cyril asks:

> Can the correction be expressed entirely as exact source-ID-set aggregation of already formed human-decision candidates, without altering comparative Evidence formation or introducing semantic deduplication?

Yes. The current unresolved relationship already carries supporting Observation IDs. Those IDs resolve against the existing Observation array, and each matched Observation already carries exact `sourceId`. Distinctness, sorting, and set serialization are local deterministic operations.

No extraction field, metadata, type, parser, provider, repository contract, rendering responsibility, service, component, dependency, or architecture change is required.

**Cyril finding:** `ONE LOCAL QUESTION-KEY REPLACEMENT CAN USE EXACT EXISTING SOURCE PROVENANCE WITHOUT TOUCHING EVIDENCE FORMATION`.

## 22. Combined Outcome

**OUTCOME 1 - ONE LOCAL HUMAN-DECISION-AGGREGATION CORRECTION AUTHORISED**

Outcome 2 is not selected because the settled review and current private provenance resolve the aggregation dependency exactly.

Outcome 3 is not selected because the current failed attempt remains within the previously authorised per-disagreement responsibility and requires only a smaller local delta.

Outcome 4 is not selected because the failed Evidence, settled boundary, current source, and focused fixtures are sufficient to define and falsify the correction.

## 23. Exact Non-Consequences

This review does not:

1. edit production or tests;
2. consume correction Authority;
3. implement, execute, validate, evidence, or accept the correction;
4. run Jest, adjacent tests, typecheck, or lint;
5. invoke Andy;
6. inspect or assemble the real programme manifest;
7. execute a synthetic or real contribution;
8. modify the failed multi-disagreement Evidence;
9. create the fresh correction Evidence;
10. create an acceptance review;
11. modify or reconsider contribution Authority;
12. change Theory, architecture, organisational truth, programme priority, or human decision ownership;
13. claim capability, production readiness, real-source compatibility, programme correctness, Formation completion, certification, or contribution readiness.

## 24. Stop State

`DOCUMENTATION-ONLY AUTHORITY REVIEW COMPLETE - OUTCOME 1 - CORRECTION AUTHORITY UNCONSUMED`

Human-decision aggregation correction Authority review stops here.