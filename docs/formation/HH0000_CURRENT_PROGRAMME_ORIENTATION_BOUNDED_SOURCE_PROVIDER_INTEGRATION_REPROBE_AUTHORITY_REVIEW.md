# HH-0000 Current Programme Orientation Bounded Source-Provider Integration Re-Probe Authority Review

**Status:** OUTCOME 1 - ONE EXACT SYNTHETIC INTEGRATION RE-PROBE AUTHORISED
**Decision date:** 2026-08-12
**Review type:** Documentation-only MARC and Cyril re-probe Authority assessment
**Implementation effect:** None during this review - one future exact test-input amendment is authorised in Section 10
**Production effect:** None
**Contribution effect:** None
**Programme-source effect:** None - no real manifest or programme record may be used

## 1. Governing Question

> May one fresh bounded Authority permit the existing synthetic test to replace only its failed neutral integration input with the source-established definition-shaped input, execute the focused probe once, and acquire the previously missing Andy-integration Evidence without changing production, direct-provider behavior, programme access, or contribution status?

**Answer:** Yes, only within the exact amendment, single execution, and Evidence boundary in Section 10.

This review does not edit or execute the test, try the new question, modify production, inspect programme records, perform acceptance, or begin contribution.

## 2. Immediate Basis and Traceability

The immediate controlling review is:

1. `docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_ANDY_REPOSITORY_RETRIEVAL_ACTIVATION_BOUNDARY_REVIEW.md`.

The existing synthetic Evidence is:

1. `docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_BOUNDED_SOURCE_PROVIDER_SYNTHETIC_TEST_EVIDENCE.md`.

The original synthetic-test Authority is:

1. `docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_BOUNDED_SOURCE_PROVIDER_SYNTHETIC_TEST_AUTHORITY_REVIEW.md`.

**Principle:** Truth before certainty, evidence before claims, smallest justified change, and no contribution without human Authority.

**Theory:** `docs/theory/004-THEORY-OF-JUDGEMENT.md`.

**Architecture:** Existing public `AndyDigitalColleague.runConstitutionalExamination` route and retained repository-service injection boundary.

**Engineering:** One atomic test-input literal replacement and one focused execution; no provider, assertion, fixture, manifest, or production change.

**Milestone:** Not Applicable.

**Evidence:** Prior direct-provider Evidence, failed integration Evidence, and the completed source-only activation-boundary review.

## 3. Authority Preconditions

Mechanical checks before this decision established:

1. all three governing records exist;
2. the activation-boundary review selected Outcome 1;
3. the previous failure is classified `A. TEST INPUT DID NOT ENTER THE EXISTING RETRIEVAL CLASS`;
4. the old input literal currently appears exactly twice in `lib/academy/__tests__/boundedSourceProvider.test.ts`;
5. the proposed new literal does not yet appear;
6. this Authority path and the future Evidence path were unused;
7. the covered production files have no working-tree change;
8. no test or Andy invocation was executed during this review.

## 4. Prior Evidence Preserved

These findings remain settled unless fresh material Evidence contradicts them:

**Direct provider closure:** DEMONSTRATED FOR THE AUTHORISED SYNTHETIC PROVIDER CASES.

**Andy integration closure:** NOT YET DEMONSTRATED.

**Previous integration failure:** A. TEST INPUT DID NOT ENTER THE EXISTING RETRIEVAL CLASS.

The prior Evidence record is historical Evidence and must remain unchanged. The previous failure is not reclassified as a production defect.

The same focused test necessarily executes its existing direct-provider assertions before the integration probe. That incidental re-execution is permitted because no direct-provider assertion or mechanism may change. It does not erase, replace, or self-accept the prior direct-provider Evidence.

## 5. Exact Amendment Under Review

The current test contains the exact old sentence twice:

> Explain the fictional lantern catalogue boundary.

The two occurrences are:

1. the argument supplied to `runConstitutionalExamination`;
2. the expected query-history value that asserts what the provider received.

The only authorised amendment is one atomic exact-literal replacement at both existing occurrences with:

> What does the fictional lantern catalogue boundary mean?

Updating the expected query-history literal is part of the same input replacement. It preserves the existing assertion and is not assertion weakening.

No other text, formatting, import, provider logic, fixture, manifest, spy, assertion, test structure, or file content may change. If the two-occurrence exact replacement is insufficient, implementation must stop.

## 6. Legitimacy of the Replacement Input

The completed activation-boundary review establishes from current source that:

1. `what does` enters the existing `definition` class;
2. `definition` sets `shouldRetrieve: true`;
3. the ordinary contextual non-retrieval return is guarded by `!shouldRetrieve` and is therefore bypassed;
4. the input is not a review, recommendation, priority, or advice request;
5. the later structured planner treats `what does` as `explain`;
6. the input contains only a fictional lantern-catalogue concept and no real programme content;
7. it requests no priority, Action, founder intent, relational Understanding, programme judgement, or contribution;
8. the public route and production behavior already exist and are not manipulated or changed by choosing this input.

This is an honest neutral use of an existing repository-grounded mode. It is not a disguised real task for Andy.

## 7. Exact Claim Authorised for Acquisition

The future re-probe may establish only:

> Through an existing neutral retrieval-eligible public route, Andy uses the injected one-use closed provider, receives only its authorised synthetic source set, preserves source provenance, does not broaden repository access, and does not create prohibited persistent or consequential effects.

The re-probe may not establish:

1. real programme contribution readiness or Authority;
2. correctness or sufficiency of a real manifest;
3. formation completion or relational Understanding;
4. repository capability generally;
5. closure of the default `RepositoryKnowledgeService`;
6. production readiness or a production capability;
7. milestone completion, certification, deployment readiness, or acceptance.

## 8. Required Runtime Falsifiers

The one focused execution must attempt to falsify all of these integration facts through the existing assertions:

1. `retrievalActive === true`;
2. Andy invokes the injected provider exactly once;
3. no fallback repository service is invoked;
4. only manifest members A and B are returned;
5. C and D remain unavailable and unaccessed;
6. Andy adds no source beyond the provider result;
7. returned `id`, `sourcePath`, section, exact text, and fragment remain attributable under the existing test contract;
8. public Memory remains empty;
9. `getLastReflection()` remains `null`;
10. no Learning promotion occurs;
11. no feedback event occurs;
12. no automatic follow-up or second turn occurs;
13. no repository or external write occurs beyond test-owned temporary setup;
14. the integration provider records exactly one successful search from Andy;
15. a second use of that same provider is refused without another source access.

Source inspection may explain these assertions but may not replace runtime Evidence where the test can observe the fact.

## 9. Side-Effect and Failure Boundary

One transient private conversation-history entry may be created by the single public invocation. It must remain inside the fresh Andy instance, must not become public Memory, Learning, feedback, contribution, or durable state, and must be discarded with the instance.

If the definition route creates Reflection, public Memory, Learning, automatic follow-up, recommendation, an additional search, an external write, or another prohibited effect, the test must fail.

If the focused test fails for any reason:

1. do not change production;
2. do not change the query again;
3. do not alter the provider, fixtures, manifest, spies, or assertions;
4. do not suppress the effect or add another mechanism;
5. record the exact failure in the fresh Evidence record;
6. stop without a repair pass or second Jest execution.

## 10. Exact Authority Granted

After this review ends, Authority is granted only to:

1. modify `lib/academy/__tests__/boundedSourceProvider.test.ts` by replacing the exact old input literal at its two existing occurrences with the exact new input literal in Section 5;
2. preserve every provider, fixture, manifest, spy, assertion, and structural line otherwise unchanged;
3. execute exactly once:

```text
npm test -- --runInBand lib/academy/__tests__/boundedSourceProvider.test.ts
```

4. if that focused execution passes, run exactly once each:

```text
npm run typecheck
npx eslint lib/academy/__tests__/boundedSourceProvider.test.ts
```

5. obtain editor diagnostics for the test and fresh Evidence record;
6. perform focused static checks for the exact replacement, unchanged production, unchanged provider and assertions, prohibited content, temporary-only fixture writes, and task-file boundary;
7. create exactly one fresh Evidence record at `docs/formation/HH0000_CURRENT_PROGRAMME_ORIENTATION_BOUNDED_SOURCE_PROVIDER_INTEGRATION_REPROBE_EVIDENCE.md`;
8. record exact commands, outputs, invocation counts, attempted and returned paths, provenance, prohibited-effect results, failures, limitations, unknowns, and withheld claims;
9. stop for fresh independent acceptance.

If the focused execution fails, Authority remains only to create the fresh Evidence record, obtain diagnostics for that record, perform non-executing static and whitespace checks, and stop. Typecheck, ESLint, a second query, and another Jest execution are not authorised after failure.

## 11. Authority Withheld

Authority is withheld to:

1. modify any line other than the two exact old input-literal occurrences;
2. weaken, remove, reorder, or add an assertion;
3. change provider implementation, one-use behavior, instrumentation, fixture, manifest, synthetic document, spy, or cleanup;
4. modify production source, interface, classification, retrieval, search, ranking, or state behavior;
5. edit the prior Evidence, original Authority, activation-boundary review, or any other existing record;
6. create a new provider, helper, test, query, hook, or mechanism;
7. run a second query, alternative input, repair pass, second Jest execution, or broad suite;
8. create or inspect a real programme manifest, source package, or programme record;
9. ask or paraphrase the real programme-orientation question;
10. use Context Door or natural input;
11. create or promote Memory, Learning, feedback, Reflection, follow-up, contribution, or durable conversation state;
12. repair any behavior exposed by the re-probe;
13. claim production defect, capability, readiness, completion, certification, contribution Authority, or acceptance from the result alone.

## 12. MARC Independent Review

**Question:** Is changing only the synthetic sentence from a non-retrieval explanation shape to the existing neutral definition shape still a fair access-boundary test rather than manipulating Andy into performing work he was not asked to do?

**Finding:** Yes. The concept remains fictional and non-consequential. The new wording asks only for a definition from the already supplied neutral source boundary. It does not ask Andy to advise, recommend, interpret a person, exercise founder intent, judge a real programme, or contribute to Helping Hand. The answer remains disposable integration output with no formation or relational meaning.

**MARC conclusion:** `EXACT NEUTRAL DEFINITION RE-PROBE REMAINS A FAIR NON-CONTRIBUTION ACCESS TEST`.

## 13. Cyril Independent Review

**Question:** Does replacing only the failed input exercise the already-existing retrieval path without changing the provider, assertions, production source, or behavioral contract?

**Finding:** Yes. The amendment changes only which existing public input class the probe exercises. Replacing the invocation literal and its matching query-history expectation preserves the assertion contract; it does not weaken the test. No provider repair, production correction, test hook, architecture change, or new mechanism is required.

**Cyril conclusion:** `EXISTING RETRIEVAL PATH MAY BE EXERCISED BY ONE EXACT INPUT REPLACEMENT`.

## 14. Authority Outcome

**OUTCOME 1 - ONE EXACT SYNTHETIC INTEGRATION RE-PROBE AUTHORISED**

Outcome 1 is justified because:

1. source inspection identifies the exact reason for the earlier non-retrieval result;
2. the proposed definition input uses an existing neutral public route;
3. the test claim, provider, fixtures, manifest, instrumentation, and assertions remain unchanged;
4. the only textual amendment is the exact old-to-new input replacement at its two existing occurrences;
5. one execution can acquire the previously missing integration Evidence;
6. failure remains valid Evidence and grants no repair Authority;
7. no production, programme, formation, relational, or contribution consequence follows.

This decision authorises Evidence acquisition only. It does not establish integration closure or accept any future result.

## 15. Independent Acceptance Still Required

Passing runtime assertions, typecheck, ESLint, diagnostics, and static checks would be claim-bearing Evidence, not self-acceptance.

A fresh independent review must decide whether the re-probe actually establishes exact integration closure, provenance retention, no wider access, no prohibited persistent effects, one-use refusal, and conformity to this Authority.

Only that later review may accept or reject the Evidence. It still may not grant real programme contribution Authority by implication.

## 16. Stop

Do not edit or run the test during this Authority review.

Do not try the new question, modify production, create a real manifest, inspect programme records, ask Andy the real question, perform acceptance, or begin contribution.

Integration re-probe Authority review stops here.